Primero deber crear un entorno virtual
python -m venv .venv
con ese comando depsues debes instalar las loibrerias al entorno virtual aun que antes de eso debes  usar este comadno tambien 
.\.venv\Scripts\activate
una vez activado el entono virtual debes instalar las librerias deberas poner en la consola

pip install -r requeriments.txt
